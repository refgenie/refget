export POSTGRES_HOST="localhost"
export POSTGRES_DB="seqcol"
export POSTGRES_USER="seqcol"
export POSTGRES_PASSWORD="x9t72)bn%sk23["
export POSTGRES_TABLE="seqcol"

export SEQCOLAPI_PORT="8100"
export SERVER_ENV="dev"

# Cloud storage credentials (per-provider, uses {CLOUD}_ACCESS_KEY naming)
export BB_ACCESS_KEY=`pass b2-cloud/b2_access_key_id`
export BB_SECRET_KEY=`pass b2-cloud/b2_secret_access_key`
# export AWS_ACCESS_KEY=`pass aws/access_key_id`
# export AWS_SECRET_KEY=`pass aws/secret_access_key`

# Cloud storage configuration for FASTA uploads (optional, requires boto3)
# Credentials are resolved from {CLOUD}_ACCESS_KEY env vars based on "cloud" field
export FASTA_STORAGE_LOCATIONS='[{"bucket": "cloud-databio", "prefix": "fasta/", "cloud": "bb", "region": "us-west-002", "endpoint": "s3.us-west-002.backblazeb2.com", "url_base": "https://cloud2.databio.org/"}]'

